name: Deploy prod buy runner

on:
  push:
#    branches: [ "no-ci" ]
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
#  prepare-environment:
#    runs-on: self-hosted
#    steps:
#      - name: checkout repo
#        uses: actions/checkout@v3
#      - name: Stop and remove containers, networks
#        run: docker-compose down
#      - name: Remove backend image
#        run: docker rmi memnik:0.0.1

  deploy:
    runs-on: self-hosted
#    needs: prepare-environment
    steps:
      - name: checkout repo
        uses: actions/checkout@v3
      - name: docker-compose
        run: docker-compose up -d --build client-backend
